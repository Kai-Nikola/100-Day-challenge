import numpy as np
import math
# array = np.array([1 ,2, 3])
array = np.random.random((4, 5))
# array = int(array)
# print(array > 1)
# np.set_printoptions(threshold=np.inf)  # Display all elements
print(np.round(array))
sum_array = (np.sum(array))
print(round(sum_array))
